{
  "name": "jarvis-backend",
  "version": "1.0.0",
  "description": "Thin proxy between the Jarvis Android app and the Anthropic API. Keeps the API key server-side.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express-rate-limit": "^7.4.0",
    "@anthropic-ai/sdk": "^0.27.0"
  }
}
